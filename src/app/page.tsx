// Landing placeholder — the marketing pages are scaffolded, not built.
// The product lives at /upload once the studio pages land.
export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl flex-col items-center justify-center gap-6 px-6 text-center">
      <h1 className="text-5xl font-bold tracking-tight">
        Upload one photo.
        <br />
        See it on every product.
      </h1>
      <p className="max-w-xl text-lg text-neutral-600">
        Phone cases, mugs, posters, canvas prints and more — previewed with your
        photo in seconds, printed exactly as you approved.
      </p>
    </main>
  );
}
